import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  button: {
    backgroundColor: '#0000FF',
    borderColor: 'white',
    borderWidth: 1,
    borderRadius: 12,
    overflow: 'hidden',
    padding: 10,
  },
  text: {
    color: 'white',
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
  }
})
