import {StyleSheet} from 'react-native';
import { colors } from '../../colors';

export const styles = StyleSheet.create({
  text: {
    color: '#353535',
    marginTop: 8,
    textAlign: 'center',
    fontSize: 16,
    fontWeight: '600',
  },
});
